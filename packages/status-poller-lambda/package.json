{
  "name": "status-poller-lambda",
  "version": "1.0.0",
  "private": true,
  "description": "StatusPoller Lambda - TODO Phase 4 (Polling Pattern)",
  "type": "module",
  "scripts": {
    "build": "tsup src/interfaces/lambda-handler.ts --format esm --clean",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "contracts": "workspace:*",
    "database-adapter-postgres": "workspace:*",
    "queue-adapter-sqs": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/aws-lambda": "^8.10.145",
    "tsup": "^8.2.0",
    "typescript": "^5.8.3"
  }
}
