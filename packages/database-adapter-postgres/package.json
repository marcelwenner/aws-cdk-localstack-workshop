{
  "name": "database-adapter-postgres",
  "version": "1.0.0",
  "private": true,
  "description": "PostgreSQL database adapter for workshop",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "contracts": "workspace:*",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/pg": "^8.11.6",
    "tsup": "^8.2.0",
    "typescript": "^5.8.3"
  }
}
