{
  "name": "contracts",
  "version": "1.0.0",
  "private": true,
  "description": "Shared contracts (ports, DTOs, types) for workshop",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "prepare": "tsup src/index.ts --format esm --dts --clean",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsup": "^8.2.0",
    "typescript": "^5.8.3"
  }
}
